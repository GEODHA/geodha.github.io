# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files - NEVER commit these
.env
.env.local
.env.production
.env.staging
.env.development.local
.env.test.local
.env.production.local

# Firebase private key files
**/firebase-adminsdk-*.json
**/service-account-*.json
**/*serviceAccount*.json
**/*service_account*.json

# API key files
**/*-key.json
**/*-keys.json

# Setup / internal docs (contain instructions + placeholder secrets)
FIREBASE_SETUP.md

# Storage CORS config — applied once via Firebase CLI, not needed in repo
cors.json

# Admin panel — local only, not committed
# Lives in src/admin/ and is loaded via dynamic imports in App.tsx
src/admin/

# Seed script — run once locally to populate Firestore, not needed in repo
scripts/seed_geodha.ts

# Reference and documentation files

# Moved documentation files

# Legacy reference code
cleanup-history.ps1
passwords.txt
SECURITY-INCIDENT-RESPONSE.md
bfg-1.15.0.jar

# Temporary scripts and development tools
scripts/migrate_legacy_reports.ts
scripts/check_firebase_state.ts
scripts/setup_heatmap_config.ts
scripts/debug_database.ts
scripts/fix_firebase_data.ts
scripts/fix_firebase_data_v2.ts
scripts/update_legacy_author_names.ts
scripts/analyze_missing_images.cjs
data/ward-heatmap2.json
